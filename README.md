# Caching http microservice

# Installation

Copy and setup env
```bash
cp .env.example .env
```

Fetch dependencies
```bash
docker-compose run --rm app yarn add express
```

Up server
```bash
docker-compose up -d
```
